# URL base del backend (sin trailing slash)
VITE_API_URL=http://localhost:8000/api
# Para despliegue en Render, use /api si el backend se sirve a la misma URL.
# VITE_API_URL=/api

# Nombre de la app (usado en títulos y branding)
VITE_APP_NAME=TecnoCare
